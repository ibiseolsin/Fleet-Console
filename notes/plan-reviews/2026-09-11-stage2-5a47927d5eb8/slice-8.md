# 2026-09-11-stage2-5a47927d5eb8 — slice 8 검증

구현 세션: Claude Code, Opus 5 (1M context, `claude-opus-5[1m]`) · 워크스페이스 `slice8` · 기준 HEAD `c148272`.
자기 담당 지적만 확인했다 (R2 · R3 · R7, 그리고 R6 중 슬라이스 8 몫). `review.md` 와 다른 워커 파일은 고치지 않았다.

| id | 결과 분류 | 변경 전 증거 | 변경 후 증거 | 코드 상태/비고 |
|---|---|---|---|---|
| R2 | 실행 확인 | 계획대로면 시나리오 3을 "규칙 밖" 으로 표본에서 뺐다 → 재생 8개. `observeFleet` 기준 관찰(planDirty 미전달) 실행: beacon 4번 `eligible=true`("병렬 가능") · 자격 있는 슬라이스가 남아 파견이 막히지 않는다 | `observeFleet({ planDirty })` 로 재생: beacon 4번·cobalt 2번이 `PLAN.md 미커밋 변경 — 커밋(\`/plan-review\`)이 파견 신호` 로 보류, 자격 있는 슬라이스 0개. 문장은 `planDirtyBlock` 이 낸 것 그대로(임시 저장소를 미커밋 상태로 만들어 직접 호출, 깨끗할 때 `null` 확인). 재생 9개 / 일치 9 | `src/fleet/source.mjs` 에 `planDirty` 인자만 열었다(`dispatchPlan` 이 이미 받는 것). 시나리오 3은 별도 관찰 패스 — 4·5·10 과 섞지 않았다. `hold` 는 `other`(HOLD_CODES 에 그 접두어가 없다), 대조는 사유 문장으로 한다 |
| R3 | 실행 확인 | 워크스페이스 상태로 대조하는 방식을 실행: beacon/slice3 `state=busy`, `counts.waiting=0` → 기대 `waiting` 과 **불일치**. 그대로 뒀으면 8번이 헛되이 불일치가 되고, 맞추려면 픽스처나 판정 규칙을 고쳐야 한다(`PRD.md §7` 위반) | 재개 갈래·카드로 대조: `resume[slice3].action='wait'`("워커가 결정을 기다림: …") + `cards[slice3].wait` 가 차 있음 → **일치** | 시나리오 8의 판정 재료를 `resume[].action` 과 `cards[]` 로 못 박았다. `landCheck` 의 카드 읽기는 그대로 둔다 |
| R7 | 실행 확인 | 계획의 완료 기준에 시간 출력이 없어 `PRD.md §8` "회차 소요 10초 이내" 의 값을 만들 곳이 없었다 (슬라이스 10이 요구하는 다섯 기준 중 하나가 빈다) | 시나리오마다 관찰+판정 한 바퀴를 재서 출력·저장한다. 실측: 356·373·374·389·411·412·416·435·438ms · 합계 3604ms · 가장 긴 회차 438ms · 상한 10000ms 안쪽. 상한을 넘으면 `exit 1` 로 실패시킨다 | `scripts/eval.mjs` `CYCLE_BUDGET_MS`. 시나리오 9는 재생하지 않으므로 시간이 `null` 이다 |
| R6 | 실행 확인 (슬라이스 8 몫만) | 결과가 표준출력뿐이면 `/eval` 화면이 읽을 자리가 없다 — `data/eval/` 폴더 자체가 없었다 | `npm run eval` 이 `data/eval/scenarios.json` 을 남긴다(`data/usage/cycles.json` 과 같은 꼴). 담은 것: 기준 세팅 · 개수 넷 · 시간 · planDirty 탐지 · 격리 · 시나리오 10행 | 화면에 붙이는 것은 슬라이스 10의 완료 기준이다. 슬라이스 9가 같은 폴더에 `settings.json` 을 더한다 |

## 계획에 없던 실패를 하나 더 막았다 (지적 아님)

`npm run check` 의 쓰기 도구 확인은 샌드박스에 **정말로** 파견·착륙한다. 그 뒤에 재생하면 픽스처가 달라져
9개 중 6개만 맞고 일치율이 66.7% 로 적힌다(2026-09-11 실측) — 판정이 아니라 픽스처가 틀린 것이다.
`npm run eval` 이 픽스처를 먼저 다시 만들고, `scripts/eval.mjs` 가 재생 전에 픽스처를 `FIXTURE` 정의와
대조해 다르면 `exit 2` 로 멈춘다. 멈춤을 실행으로 확인했다(정의 [slice2, slice3] · 실제 [slice3] 등 3건 보고).

## 이 슬라이스가 돌린 검사

- `npm run eval` — 재생 9 / 일치 9 / 불일치 0 / 재생 불가 1 · 격리 233개 파일 중 변한 것 없음 · exit 0
- 연속 두 번 결과 대조 — 판정·개수·격리·사유 문장까지 동일 (시각이 든 문장은 분이 바뀌면 그 문자열만 바뀐다)
- `npm run check` — 다섯 전부 통과 (첫 시도에서 `~/.sp-sync/` 격리 오탐 1건: 다른 세션이 복귀 카드를 썼다. 재실행 통과)
- `npm test` — 455/455
- `ANONYMIZATION.md §4` 스캔 — 이번 슬라이스가 만든·고친 파일 낱말 경계 기준 0건. 저장소 전체에는 1건이 남아 있다(`notes/slice8-평가재생.md` 의 "발견한 것" — 커밋 `c148272` 이 들여온 `review.md:24`, 이번 슬라이스에서 손대지 않았다)
