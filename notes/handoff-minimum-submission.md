# 무지출 과제 제출 — 다음 세션 인계

- 사용자 최종 지시: 후속이 남으면 자율적으로 계속한다. MD만 저장하고 끝내지 않는다. 새 세션·사용자 선택이 필요한 때 즉시 알린다.
- 추가 지출 금지, 교육기관 제공 OpenAI API만 최소 사용, 개발 토큰 절약. 이미 만든 기능을 재사용하고 추가 기능은 하지 않는다.
- `ASSIGNMENT.md`가 사용자 제공 평가 기준이다. URL에서 실제 에이전트 실행은 필수, 유료 호스팅/Anthropic/멀티 에이전트는 필수가 아니다.
- 본 세션은 계획 단계다. `PRD.md`, `PLAN.md`, 이 파일을 읽고 미완 범위만 최소 수정 계획으로 정리한다. 기존 완료 항목을 재조사하지 않는다.
- 규칙: 계획은 plan mode, 승인 뒤 PLAN 저장만 하고 커밋 금지. 새 최상위 세션 `/plan-review`가 검토·커밋, 표준 모델 새 세션이 구현한다.
- 계획을 저장하고 방치하지 말 것. 승인 후 Orca의 같은 프로젝트 새 검토 세션으로 실제 인계한다. 검토→구현도 실제 이어지게 지시한다. 원래 창은 구현하지 않는다.
- 기존 12번은 컨테이너 로컬 검증까지 완료된 WIP, 13번은 공개 전 검수였다. 기존 번호/히스토리를 보존하며 남은 범위만 최소로 재구성한다.
- OpenAI 전환의 핵심은 `src/agent/loop.mjs`(Claude SDK query, persisted session), `src/demo.mjs`(Anthropic 키 확인), 관련 테스트/환경 예시/화면 문구다.
- `src/fleet/tools.mjs` 도구 5개·실제 승인 게이트·파일 상태와 화면은 재사용. `sp-sync` 및 판정 규칙은 수정 금지.
- API 호출/도구 결과/승인 대기 상태를 파일로 보존해 OpenAI에서도 이어갈 수 있게 한다. 가짜 의존성 검증부터 하고 실제 키 검증은 대표 실행 최소 횟수만.
- 후보 `gpt-4.1-mini`: 도구 호출 지원, 입력/캐시/출력 1M당 0.40/0.10/1.60 USD. 교육기관 키 사용 가능 여부는 미확인, 모델은 환경변수로 변경 가능하게.
- 공식 API 근거: https://developers.openai.com/api/docs/guides/function-calling 및 https://developers.openai.com/api/docs/models/gpt-4.1-mini (2026-09-11 열람).
- 현재 `OPENAI_API_KEY`, `OPENAI_BASE_URL`, 모델 환경변수와 로컬 `.env`가 없다. 키값을 채팅/로그로 요구하지 말고 필요한 단계에 사용자에게 로컬 설정을 요청한다.
- 배포 질문 사용자 답변 확정: **PC 없이 동작하는 무료 호스팅 필요**. 로컬 PC 상시 실행·터널 방식은 제외한다.
- 무료 호스팅의 절전/재시작 중 상태 보존과 실행 한도를 충족하는 최소 변경을 확인한다. 추가 결제는 금지이며 실제 공개 전 내용 검수와 배포 승인 필요.
- Render Free는 15분 유휴 후 절전하며 재시작/절전마다 파일 손실. 유료 디스크는 쓰지 않는다. https://render.com/docs/free (열람).
- Cloudflare Quick Tunnel은 무료 임의 공개 URL, 로컬 서버 필요, 200 동시 요청, SSE 미지원, SLA 없음·개발/테스트용. https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/do-more-with-tunnels/trycloudflare/ (열람).
- Cloudflare 로컬 터널은 위 사용자 답변으로 후보에서 제외했다. 같은 PC 운영 질문을 다시 하지 않는다.
- 마지막 단계는 실제 URL에서 승인/작업/재개 검증, 결과물 캡처와 README. 기존 실측치를 새 모델 실측으로 표시하지 않는다.
- 전체 익명화 기존 23파일 156줄 패턴 일치가 남아 있다. 새로운 변경 파일은 0건이었다. 실제 자유문장 공개 검수는 미완, 공개 승인 없이 외부에 서비스/저장소를 노출하지 않는다.
- 기반 커밋: a59741b 컨테이너 WIP, f3e971a 무지출 제약, f307d9b 평가기준. 최근 검사: Windows 455/455, Linux 앱 check/빌드/재시작 통과. Linux 벤더 13실패·2skip은 기록돼 있다.
- 최종 단계 게이트에서만 사용자에게 필요한 선택/승인 요청. 계정 준비나 키가 없다고 가짜 검증·코드 준비 등 독립 작업을 멈추지 않는다.
- 2026-09-11 계획 반영 완료(최상위 세션 plan mode, 승인됨): PLAN.md 2단계에 14(OpenAI 전환) → 12(Render Free + GitHub 스냅샷 상태 보존) → 13(수용·제출물·공개 검수) 로 고쳐 씀. 커밋은 하지 않았다.
- 인계: 같은 워크트리의 새 `/plan-review` 세션(최상위 모델)이 검토·수정·커밋하고, 커밋 뒤 회차가 14 를 파견하는지 확인한다. 조사 근거(무료 호스팅 비교)는 슬라이스 12 본문에 URL 로 남겼다.
