# PRD — Fleet Console

작성 2026-09-10. 근거: `PROBLEM.md`. 구현 순서는 `PLAN.md`.
사용자 관점만 적는다 — 기술 스택과 내부 설계는 여기 넣지 않는다.

**2026-09-11 사용자 제약 변경:** 추가 지출과 과도한 개발 토큰 사용을 피하고 과제 평가 기준을 충족하는
최소 범위만 진행한다. AI 호출이 꼭 필요한 경우 교육기관 제공 OpenAI 키를 최소 사용한다.
과제 원문과 필수 범위는 `ASSIGNMENT.md`에 있다. 기존 배포·성공 기준을 추가 지출의 근거로 삼지 않는다.

> **공개 저장소다.** 들여오는 코드·기록·측정치는 `ANONYMIZATION.md` 의 규칙을 통과해야 한다.
> **저장소를 공개로 바꾸는 것은 소유자 승인 없이 하지 않는다** — 비공개 원격까지는 열려 있다.

## 1. 문제 정의

여러 AI 코딩 에이전트를 동시에 굴리는 개발자는 회차마다 다섯 가지를 판단해야 한다 —
착륙 · 재개 · 인계 · 파견 · 사람 호출. 이 판단은 이미 규칙으로 자동화돼 있지만,
**그 결과를 보고 승인하는 통로가 로컬 마크다운 파일뿐이다.**

Fleet Console 은 그 통로를 연다.

| | 입력 | 결과물 |
|---|---|---|
| 사람에게 | 운영 회차 기록 · 승인 요청 | 화면에서 본 플릿 현황, 누른 승인, 되짚을 수 있는 실행 근거 |
| 외부 에이전트에게 | "이 플릿의 다음 할 일을 정해줘" | 도구로 조회한 상태 · 자격 판정 · (승인 후) 실행된 파견·착륙 |

## 2. 타겟 유저

**여러 AI 코딩 에이전트를 병렬로 운영하는 개발자.**

- 진행 중인 저장소가 3개 이상이고, 작업을 슬라이스 단위로 쪼개 에이전트에게 맡긴다
- 에이전트를 2종 이상 섞어 쓴다(사용량 한도가 갈리므로 인계가 필요하다)
- 자리를 비우는 동안에도 플릿이 돌기를 원하지만, 병합과 비용 발생은 자기가 확인하고 싶다

부차 사용자: **같은 플릿을 넘겨받는 사람.** 지금 무엇이 왜 멈춰 있는지 링크 하나로 파악해야 한다.

## 3. 워크플로 설계

트리거부터 결과물까지. **판단은 누가 하고 실행은 무엇이 하는지** 나눈다.

| # | 단계 | 판단 주체 | 하는 일 |
|---|---|---|---|
| 0 | 트리거 | 시스템 | 워커 턴이 끝났을 때 · 30분 안전망 · 사람이 화면에서 누를 때 |
| 1 | 관찰 | **도구** | 프로젝트별 계획서·작업 공간·병합 가능 여부·사용량 한도·진행 카드를 읽는다 |
| 2 | 자격 판정 | **도구(규칙)** | 착륙/재개/인계/파견 각각에 대해 가능·불가와 **그 사유**를 낸다 |
| 3 | 우선순위 결정 | **에이전트** | 판정 결과를 받아 이번 회차에 무엇을 먼저 할지 고른다 |
| 4 | **승인 게이트** | **사람** | 비용이 발생하거나 되돌리기 어려운 실행 전에 멈춘다 |
| 5 | 실행 | **도구** | 작업 공간 생성 · 세션 기동 · 병합 · 재기동 |
| 6 | 기록 | 시스템 | 무엇을 왜 했고 얼마가 들었는지 회차 기록에 남긴다 |
| 7 | 재관찰 | — | 남은 일이 있으면 1로 돌아간다. 없거나 상한에 닿으면 종료 |

**종료 조건** — 회차당 실행 상한, 재개 시도 2회, 충돌 해소 2회, 회차 시간 상한.
넘으면 진행을 멈추고 사람이 볼 항목으로 올린다.

## 4. 도구 계획

외부 에이전트에게 노출하는 도구와 권한. **읽기와 쓰기를 분리하고, 쓰기는 전부 승인을 거친다.**

| 도구 | 하는 일 | 권한 | 실패 시 |
|---|---|---|---|
| `fleet_status` | 프로젝트별 작업 공간·진행 상태 조회 | 읽기 전용 | 그대로 오류 반환, 재시도 없음 |
| `fleet_slices` | 계획서의 슬라이스 목록과 **파견 자격 + 불가 사유** | 읽기 전용 | 그대로 오류 반환 |
| `fleet_report` | 지난 회차 기록 조회(날짜 범위) | 읽기 전용 | 없는 날짜면 빈 결과 |
| `fleet_dispatch` | 슬라이스를 에이전트에게 파견 | **승인 필요** — 세션이 생기므로 비용 발생 | 자격 미달이면 사유와 함께 거부. 부분 성공 시 생성된 것을 보고하고 멈춘다 |
| `fleet_land` | 완료된 작업을 본 저장소에 반영 | **승인 필요** — 되돌리기 어려움 | 충돌이면 최대 2회 해소 시도 후 사람에게 넘김 |

- 각 도구는 **왜 그 결과가 나왔는지**를 함께 낸다. "파견 불가" 만이 아니라 "선행 슬라이스 12가 미완"까지.
- 읽기 도구는 어떤 상태도 바꾸지 않는다. 에이전트가 마음껏 불러도 안전하다.
- 작업 공간 삭제 · 강제 푸시 · 계획서 수정은 **도구로 노출하지 않는다.**

## 5. 사람 개입 지점

| 지점 | 무엇을 보고 | 무엇을 정하나 |
|---|---|---|
| 파견 전 | 어떤 슬라이스를 어느 에이전트에게, 지금 몇 개가 돌고 있는지 | 승인 / 보류 / 다른 슬라이스 |
| 착륙 전 | 변경 요약 · 완료 체크 상태 · 검사 결과 | 승인 / 보류 |
| 충돌 해소 실패 | 무엇이 충돌했고 두 번의 시도가 어떻게 실패했는지 | 직접 처리 / 포기 |
| 결정 필요 항목 | 워커가 올린 질문, 착수 전 갈리는 결정 | 답을 준다 |
| 상한 도달 | 어느 상한에 왜 닿았는지 | 상한을 올린다 / 이번 회차를 접는다 |

승인 대기는 **화면의 큐에 쌓이고**, 답을 줄 때까지 그 항목만 멈춘다 — 다른 프로젝트는 계속 돈다.

## 6. 핵심 기능 (MVP)

반드시 있어야 하는 것만.

- [ ] 플릿 현황 — 프로젝트별 진행 중 작업, 막힌 것, 사람을 기다리는 것
- [ ] 회차 기록 열람 — 회차별 착륙/파견/막힘/결정필요, 각 판정의 **사유**까지
- [ ] 승인 큐 — 대기 항목 목록, 승인/보류, 처리 후 결과 확인
- [ ] 비용·시간 패널 — 회차별 소요 시간과 토큰, 어느 단계가 병목인지
- [ ] 도구 5종을 외부 에이전트가 붙여 쓸 수 있는 형태로 제공
- [ ] 데모 모드 — 실제 기계 없이도 링크만으로 전 과정을 재생할 수 있다

### 화면

| 경로 | 화면 | 담는 것 |
|---|---|---|
| `/` | 대시보드 | 프로젝트별 현황 카드, 사람을 기다리는 항목 배지 |
| `/runs` | 회차 목록 | 날짜별 `착륙 N · 파견 N · 막힘 N · 결정 필요 N` |
| `/runs/[id]` | 회차 상세 (trace) | 관찰 → 판정 → 실행 순서, 도구 호출과 그 사유, 소요 시간 |
| `/approvals` | 승인 큐 | 대기 항목, 근거, 승인/보류 |
| `/eval` | 평가 | 회차별 시간·토큰·비용, 세팅별 비교표, 실패 사례 |

## 7. 안 할 것

- `sp-sync` 코드 수정
- 판정 규칙 신설·변경
- 배포본에서 **남의** 저장소 조작 (배포본이 실제로 조작하는 것은 저장소 안의 샌드박스 픽스처뿐이다)
- 작업 공간 삭제, 강제 푸시, 계획서 자동 수정
- 로그인·다중 사용자 · 실시간 협업
- **제외 목록 프로젝트의 내용 반입** — 이름만이 아니라 기록·사례·측정치까지 (`ANONYMIZATION.md`)
- **승인 없는 공개 전환**

## 8. 성공 기준

측정 가능한 문장으로.

| 기준 | 목표 | 재는 법 |
|---|---|---|
| 판정 일치율 | 반입한 회차 기록을 재생해 판정이 당시 결과와 **90% 이상** 일치 | 기록의 착륙/파견/막힘과 재생 결과 대조 |
| 헛호출률 | 사람을 부른 항목 중 실제로 사람 판단이 필요했던 비율 **80% 이상** | 승인 큐 항목을 사후 분류 |
| 회차 소요 | 회차 하나의 판정이 **10초 이내** | 재생 시간 측정 |
| 관찰 가능성 | 회차 상세에서 **모든 도구 호출과 사유**를 볼 수 있다 | 재생한 회차 전부에서 확인 |
| 배포 | 링크만으로 접속해 샌드박스 회차를 **끝까지 실행**할 수 있다 | 다른 기기에서 확인 |

## 9. 평가 시나리오 세트

새로 만들지 않는다 — 실제 기록에서 뽑는다.

| # | 시나리오 | 출처 | 기대 |
|---|---|---|---|
| 1 | 정상 착륙 | 실제 회차 기록 | 완료 체크 + 커밋 + 유휴 → 착륙 승인 요청 |
| 2 | 미완인데 유휴 | 2026-09-10 회차의 막힘 항목 | 착륙하지 않고 "막힘"으로 보고 |
| 3 | 계획 미커밋 상태의 파견 | 사고 기록 (미검토 계획이 파견됨) | 파견 차단 + 사유 표시 |
| 4 | 선행 의존 미충족 | 계획서의 선행 표시 슬라이스 | 파견 자격 없음 + 어느 슬라이스 때문인지 |
| 5 | 동시 실행 상한 도달 | 상한 3 설정 | 넷째 슬라이스 파견 보류 |
| 6 | 한도로 막힌 워크스페이스 | 재개 대기 기록 | 초기화 시각 전이면 대기, 후면 재개 |
| 7 | 재개 2회 소진 | 재시도 상한 | 사람에게 넘김 |
| 8 | 카드가 질문 대기 중 | 사고 기록 (승인 문구로 착륙이 영영 멈춤) | 착륙 보류 + 결정 필요로 올림 |
| 9 | 충돌 해소 2회 실패 | 충돌 상한 | 사람에게 넘김 + 두 번의 시도 내역 표시 |
| 10 | 결정 필요 표시 슬라이스 | 계획서 태그 | 파견하지 않고 보고만 |

**세팅 비교 실험** — 동시 실행 상한(2/3/5), 재개 상한(1/2/3), 에이전트 조합(단일 / 2종 인계)을
바꿔가며 위 10개를 재생하고 일치율·소요·사람 호출 횟수를 표로 남긴다.

**실패 사례 분석** — 기존에 축적된 사고 28건을 원인 단계별로 분류한다
(관찰 단계 / 판정 단계 / 실행 단계 / 사람 개입 설계).

## 10. 미정

- [ ] 마감일 — 정해지면 슬라이스 6개를 역산
- [ ] 승인 알림 채널 — 화면 배지로 충분한지, 외부 알림이 필요한지 (MVP 밖으로 둠)
