# 회차별 시간·토큰 반입 보고

`scripts/import-usage.mjs` 가 낸다 — **손으로 고치지 않는다.** 다시 반입하면 다시 쓰인다.
규칙은 [`ANONYMIZATION.md`](../ANONYMIZATION.md), 실명·제외 목록은 로컬 전용 파일에 있다 (§5).

## 왜 새로 쟀나

회차 기록(`data/runs/`)에는 시간이 없고, 운영 저장소의 토큰 요약은 **프로젝트·주 단위**라
회차별이 없다. 그래서 플릿 로그의 줄 시각과 에이전트 세션 기록을 **회차 시각 범위로** 묶었다.

## 2026-09-14 제외 — `project-a` · `project-b`

소유자 결정으로 두 프로젝트를 통째로 제외했다(`ANONYMIZATION.md §3`, 근거는 `notes/slice13-공개검수.md`).
**회차 241 → 185** (그 둘만 담고 있던 회차 56개를 통째로 뺐다), 표 행 316개 삭제, 계측도 같은 회차만 남겼다.
회차 주소(`<파일날짜>-<순번>`)가 밀리므로 계측의 `slug` 도 다시 매겼다 — `npm run usage:check` 가 짝을 확인한다.

위 표에서 **회차 수·단계 합계·비중은 남은 185개로 다시 셌다.** 반입 과정을 설명하는 값(읽은 세션 파일,
센 메시지, 안 붙은 메시지 등)은 **반입 당시 그대로** 둔다 — 그때 실제로 그렇게 읽었다는 기록이다.

## 숫자

| | |
|---|---|
| 회차 (`data/runs/`) | **185개** (반입 당시 241개 — 아래 "2026-09-14 제외") |
| **시간이 붙은 회차** | **185개** (반입 당시 241개) |
| 로그의 회차 끝 줄 중 짝을 못 찾은 것 | 24개 (반입 당시 값) |
| **토큰이 붙은 회차** | **139개** (반입 당시 191개) |
| 읽은 세션 파일 | 561개 (시각 범위 밖이라 건너뛴 파일 240개) |
| 센 메시지 | 17305개 |
| 제외 프로젝트라 버린 메시지 | 0개 (폴더 24개) |
| 세션을 이어 열어 중복된 메시지 | 14011개 (`requestId` 로 걸렀다) |
| 어느 회차에도 안 붙은 메시지 | 8096개 (46.8%) |
| 금칙어 스캔 | **0건** |

## 시간을 어떻게 갈랐나

플릿 로그는 단계가 **끝날 때** 한 줄을 남기지 시작할 때는 안 남긴다. 그래서 구간은
"앞 줄 다음부터 이 줄까지" 이고 이름은 **그 줄의 단계**를 쓴다 — `파견 3분` 은 파견 줄 앞의
3분이고 거기에는 그 파견을 하기 전의 관찰·판정이 얼마간 섞여 있다. 마지막 구간(마지막 실행
줄 → 보고를 쓴 줄)이 `관찰·기록` 이고, 아무 실행도 없던 회차는 통째로 이것이다.

**회차 시작은 분 단위다** — 회차 제목이 `HH:MM` 까지만 적는다. 그래서 소요 시간은 최대 59초
길게 나오고, 그 오차는 첫 구간에 얹힌다. 초 단위 시작 시각은 어디에도 안 남아 있다.

| 단계 | 합계 | 비중 |
|---|---|---|
| 착륙 | 82분 21초 | 34.6% |
| 재개 | 4분 15초 | 1.8% |
| 인계 | 4분 9초 | 1.7% |
| 파견 | 87분 7초 | 36.6% |
| 관찰·기록 | 60분 3초 | 25.2% |

가장 오래 걸린 단계: **파견**.

## 토큰을 어떻게 묶었나

세션 기록의 assistant 메시지 하나는 **그 시각 이전에 시작한 회차 중 가장 최근이면서**
**그 프로젝트를 다룬** 회차에 붙는다. 그런 회차가 없거나 6시간을 넘게 떨어져 있으면
안 붙인다 (위 표의 "어느 회차에도 안 붙은 메시지"). 플릿이 다루지 않는 저장소의 세션이 여기 들어온다.

| 갈래 | 합계 | 비중 |
|---|---|---|
| 입력 | 164,680 | 0.0% |
| 출력 | 7,485,828 | 0.5% |
| 캐시 생성 | 34,166,947 | 2.3% |
| 캐시 읽기 | 1,442,739,380 | 97.2% |

## 프로젝트별 — 비중만

절대치를 안 적는 것은 로컬 매핑 파일의 "측정치" 항목이 정한 것이다 (`ANONYMIZATION.md §2`).
**제외 프로젝트를 뺀 뒤 다시 계산한 비중**이다 (§3) — 뺀 몫을 남겨 두지 않았다.
회차 기록에 이름이 안 나오는 저장소는 `기타` 로 묶는다 (반입이 새 이름을 공개하지 않는다).

| 프로젝트 | 토큰 비중 |
|---|---|
| SP-sync | 24.2% |
| 기타 | 21.8% |
| project-a | 20.5% |
| coordinator | 17.2% |
| project-b | 13.5% |
| Fleet-Console | 2.8% |

## 모델별 — 비중만

| 모델 | 토큰 비중 |
|---|---|
| `claude-opus-5` | 73.2% |
| `claude-fable-5-1` | 17.6% |
| `claude-fable-5` | 7.0% |
| `claude-sonnet-5` | 2.2% |
| `claude-opus-4-8` | 0.0% |
| `claude-haiku-4-5-20251001` | 0.0% |
| `<synthetic>` | 0.0% |

## 반입하지 않은 것

- 세션의 **내용** — 프롬프트·응답·파일 이름은 한 글자도 안 읽는다. 시각·경로·`usage` 만 본다.
- 프로젝트별·세션별 **절대치** — 비중만 낸다 (§2).
- 제외 프로젝트의 세션 — 통째로 뺐다 (§1).
