# 익명화 규칙

이 저장소는 **공개된다.** 들여오는 코드와 운영 기록에는 실제 작업 내역이 들어 있으므로,
공개 전에 아래 규칙을 통과해야 한다.

> **이 문서 자체가 공개된다.** 실제 이름·경로·목록을 여기 적지 않는다.
> 실제 값은 `anonymize-map.local.md` 에 있고 그 파일은 커밋되지 않는다.

## 0. 공개 전 승인 — 예외 없음

**저장소를 공개로 바꾸는 것은 소유자의 명시적 승인을 받고서만 한다.** 비공개 원격까지는 열려 있다.

- 원격은 **비공개로만** 만든다 (2026-09-10, 소유자 지시로 세션이 붙였다). 에이전트 세션은 저장소를 공개로 바꾸지 않는다.
- 비공개 원격으로의 push·PR·머지는 착륙 스크립트가 한다 (`~/orca/CLAUDE.md`). 비공개인 동안은 밖으로 나가는 것이 없다.
- 승인을 요청할 때 함께 제출한다:
  1. `§4 검증`의 스캔 결과 (0건이어야 한다)
  2. 지난 승인 이후 새로 추가·수정된 파일 목록
  3. 그중 외부에서 들여온 것(코드·기록·측정치)이 무엇인지
- 승인은 **그 시점의 그 내용에 대해서만** 유효하다. 파일이 추가되면 다시 받는다.
- 한 번 공개된 것은 되돌릴 수 없다 — 지워도 기록과 캐시에 남는다. 의심스러우면 넣지 않는다.

## 1. 두 갈래: 치환과 제외

| 갈래 | 대상 | 처리 |
|---|---|---|
| **치환** | 포함하기로 한 프로젝트 | 이름을 `project-a`, `project-b`… 로 **일관되게** 바꾼다. 내용은 남긴다 |
| **제외** | 제외 목록의 프로젝트 | **어떤 형태로도 넣지 않는다.** 다른 이름으로 바꿔서 싣지도 않는다 |

제외의 뜻을 좁게 읽지 않는다. 이름만 지우는 것이 아니라 **그 프로젝트에 관한 내용 자체를** 넣지 않는다 —
회차 기록의 해당 행, 사고 사례, 실측 노트, 측정치의 해당 항목 전부.

예외 하나: 들여오는 코드에서 그 이름이 **테스트 픽스처나 예시 문자열로만** 쓰인 경우.
이때는 그 이름을 중립적인 이름으로 바꾸면 된다 — 남는 것은 범용 로직이지 그 프로젝트의 내용이 아니다.

## 2. 무엇을 마스킹하나

| 대상 | 처리 |
|---|---|
| 프로젝트 이름 | `project-a` 형식으로 치환 (매핑은 로컬 파일) |
| 사용자명이 들어간 경로 | `~/…` 또는 `<HOME>/…` 로. 절대경로를 그대로 두지 않는다 |
| 원격 주소 · 저장소 URL · 호스트 별칭 | 제거 |
| ssh 설정 · 자격증명 · 토큰 · API 키 | 제거. 예시가 필요하면 명백한 더미로 |
| 사람 이름 · 이메일 · 계정 핸들 | 제거 |
| 브랜치명 · PR 번호 중 프로젝트를 특정하는 것 | 치환하거나 제거 |
| 커밋 메시지에 실명이 든 히스토리 | 들여올 때 히스토리를 가져오지 않는다 (스냅샷으로 넣는다) |
| 사용량·비용 실측치 | 비율과 상대 비교는 남기고, 절대치는 필요할 때만 |

## 3. 기록을 잘라낼 때

회차 기록에서 제외 프로젝트의 행을 지우면 그 회차의 요약 숫자가 안 맞는다.

- 행을 지운 뒤 **요약 숫자를 다시 센다.** 안 맞는 표를 그대로 두지 않는다
- 회차 자체가 제외 프로젝트 하나뿐이었다면 그 회차를 통째로 뺀다
- 평가 표의 표본 수가 줄면 **줄어든 수를 그대로 적는다.** 원래 수를 적고 각주로 빼지 않는다

## 4. 검증

커밋 전과 푸시 승인 요청 전에 돌린다. 금칙어 목록은 로컬 매핑 파일에서 읽는다.

```bash
# 추적 중인 모든 파일에서 금칙어를 찾는다. 결과가 0건이어야 한다.
git grep -n -i -E "<금칙어를 | 로 이어붙인 패턴>" -- .

# 커밋되지 않은 파일까지 포함해서 한 번 더.
grep -rn -i -E "<같은 패턴>" . --exclude-dir=.git --exclude="*.local.md"
```

- 대소문자·띄어쓰기 변형을 포함한다 (`Foo Bar`, `foo-bar`, `FooBar`, `foobar`)
- 코드 주석과 테스트 픽스처를 빼먹기 쉽다. 검사 범위에서 제외하지 않는다
- 이미지·스크린샷도 본다. 캡처에 창 제목과 경로가 찍힌다

## 5. 로컬 전용 파일

`*.local.md` 와 `*.local.json` 은 `.gitignore` 에 있다. 실제 이름·매핑·제외 목록은 거기 둔다.
이 규칙 문서에는 **패턴만** 적고 값은 적지 않는다.

반입 스크립트(`scripts/import-runs.mjs`)가 읽는 것은 `anonymize.local.json` 이다 — 치환표·제외 목록·
경로 마스킹·금칙어 패턴이 거기 있고, 설정이 없으면 스크립트는 **아무것도 반입하지 않고 멈춘다.**
기본값으로 조용히 들여오면 익명화를 건너뛴 기록이 커밋되기 때문이다.
