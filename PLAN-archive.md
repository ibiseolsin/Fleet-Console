# 작업계획 — 지난 단계 보관

끝난 단계의 본문을 옮겨 둔다. 자동화(파견·착륙·`fleet status`)는 `PLAN.md` 만 읽는다 — 여기는 사람이 되짚을 때만 본다.

## 1단계 — 판정을 도구와 화면으로 연다 (완료 2026-09-10, `d078528`~`3ae3b89`)

근거: `PRD.md`. 채점 기준: Main Quest 4 (평가 문항 5개).
2026-09-10 `/plan-review` 에서 실물 대조하고 사용자 결정 넷을 반영했다.

**그 검토에서 정한 것** (셋은 `PROBLEM.md`·`PRD.md`·`ANONYMIZATION.md` 의 기존 문장을 뒤집는다):

| 정한 것 | 왜 |
|---|---|
| 앱 안에 얇은 LLM 루프를 둔다 (슬라이스 7) | 평가 문항 3이 "에이전트 루프 구현" 을 독립 항목으로 본다. 판정 규칙은 그대로 두고 **무엇을 먼저 할지**만 LLM 이 고른다 |
| 배포본은 샌드박스 저장소 위에서 **진짜로 실행**한다 | 평가 문항 5의 "링크만으로 워크플로를 실행" |
| 비공개 원격을 **지금** 붙인다 | 원격이 없으면 착륙이 아무것도 머지하지 못한다. 승인 게이트는 "공개 전환" 으로 옮겼다 |
| 회차별 시간·토큰 계측을 새로 만든다 (슬라이스 6) | 평가 문항 4의 "각 실행의 소요 시간·토큰·비용". 기존 측정치는 프로젝트·주 단위다 |


- [x] **1. 저장소 뼈대와 sp-sync 벤더링** (2026-09-10, `notes/slice1-벤더링.md`) — `sp-sync/` 를 **익명화 치환 외에는 수정 없이** 들여온다(히스토리는 가져오지 않는다). 완료 기준: `node --test "sp-sync/test/*.test.mjs"` 가 **455개 통과 · 실패 0**(2026-09-10 원본 실측치. 원본이 늘었으면 원본에서 다시 세어 그 수를 적는다). 스크립트 한 줄에서 `dispatchPlan` 을 import 해 샘플 슬라이스 배열로 호출한 결과가 출력된다. `ANONYMIZATION.md §4` 스캔 **0건**.
  - 함정: 실명 출현은 **356회 · 23파일**이다(2026-09-10 재측정, 이름별 내역은 로컬 매핑 파일). 매핑에 적힌 280회는 이름 하나(94회)가 통째로 빠진 값이고, 반대로 매핑이 18회로 잡은 이름 하나는 코드에 **0회**다 — 치환 전에 매핑을 실제로 다시 센다.
  - 함정: 94회짜리 이름은 기본 **설정값** 셋에도 박혀 있다(`lib/common.mjs` 의 `fleetNoDispatch` · `cycleRunsDir` · `fleetPrecheck`). 문자열만 바꾸면 벤더링본이 실제 운영 경로를 가리키므로, 치환하면서 이 저장소의 샌드박스 경로로 맞춘다.
  - 테스트가 `'../sp-sync.mjs'` 를 상대경로로 import 하므로 `sp-sync/` 폴더 구조를 그대로 둔다.

- [x] **2. 샌드박스 플릿 픽스처** (2026-09-10, `notes/slice2-픽스처.md`) — 가짜 프로젝트 2~3개(계획서·작업 공간·세션 기록·진행 카드)를 저장소 안에 만들어 판정 함수 셋을 전부 부를 수 있게 한다. 완료 기준: `landCheck` · `resumePlan` · `dispatchPlan` 을 픽스처로 호출한 결과가 **착륙 자격 있음 / 막힘 / 파견 보류** 세 갈래로 각각 한 번 이상 나오는 것을 스크립트 하나가 출력한다. 실행 전후 `~/.sp-sync/` 의 파일이 하나도 변하지 않음을 해시로 확인한다.
  - 함정: `landCheck` 는 "부수효과 없음" 이라는 주석과 달리 **디스크를 직접 읽는다**(`existsSync` · 진행 카드 파일 · git). 순수 객체로 부를 수 있는 것은 `dispatchPlan` 뿐이라, 픽스처는 실제 폴더와 실제 git 저장소여야 한다. 상태 폴더는 `os.homedir()` 고정이라 환경변수로 옮길 수 없다.

- [x] **3. MCP 읽기 도구 3종** (2026-09-10, `notes/slice3-읽기도구.md`) — `fleet_status`, `fleet_slices`, `fleet_report`. 입출력 스키마와 description 을 정의하고 판정 불가 사유를 결과에 싣는다. 완료 기준: MCP 클라이언트 목록에 셋이 뜨고, 픽스처를 대상으로 호출해 슬라이스 목록과 **파견 불가 사유**가 서로 다른 세 가지(선행 미완 · 상한 도달 · 결정 필요)로 각각 한 번 이상 돌아온다. 호출 전후 픽스처 해시가 같다.

- [x] **4. MCP 쓰기 도구 2종 + 승인 게이트** (2026-09-10, `notes/slice4-쓰기도구.md`) — `fleet_dispatch`, `fleet_land`. 완료 기준: 승인 없이 부르면 대기 항목만 생기고 작업 공간은 생기지 않는다. 승인 뒤 재호출하면 픽스처에 실제로 반영된다. 자격 미달 슬라이스는 사유와 함께 거부된다. **격리 확인** — 호출 전후 `~/.sp-sync/fleet-run.lock` · `fleet-trigger.json` · `fleet-cycle-result.*.json` 이 변하지 않는다.
  - 함정: sp-sync 를 그대로 부르면 도는 플릿의 실제 락과 트리거를 건드린다. 도구 층에서 상태 경로를 주입하거나 실행부를 픽스처 전용으로 감싼다.

- [x] **5. 회차 기록 반입과 trace 뷰어** (2026-09-10, `notes/slice5-회차반입.md`, 반입 241회차) — 기록을 익명화해 들여오고 관찰 → 판정 → 실행 순서로 보여준다. 완료 기준: 반입한 회차가 목록에 뜨고(**반입한 수를 그대로 적는다**), 하나를 열면 착륙/파견/막힘/결정필요 표와 각 판정의 사유가 보인다. 폭 400px 에서 가로 스크롤이 없다. `ANONYMIZATION.md §4` 스캔 0건이고 §3대로 요약 숫자를 다시 셌다.
  - 함정: 원본 `runs/` 는 **파일이 17개, 그 안의 회차가 258개**다. 회차가 있는 파일은 10개이고 나머지 7개는 점검 노트다. 제외 프로젝트 언급이 229줄(2026-09-03 · 09-04 · 09-08 에 집중)이라, 행을 지운 회차는 요약 숫자를 다시 세고 제외 프로젝트뿐이던 회차는 통째로 뺀다.

- [x] **6. 승인 큐와 회차별 시간·토큰 계측** (2026-09-10, `notes/slice6-승인큐와계측.md`, 시간 241/241 · 토큰 191/241 · 병목 파견 38.1%) — 대기 항목을 화면에서 승인·보류하고, 회차별 소요 시간과 토큰을 본다. 완료 기준: 슬라이스 4가 만든 대기 항목이 화면에 뜨고 승인 버튼으로 처리된다. 회차별 시간과 토큰이 표로 보이고 **가장 오래 걸린 단계**가 드러난다.
  - 함정: 기존 측정치(운영 저장소의 2026-09-08 토큰 사용량 요약. 경로는 로컬 매핑 파일)는 **프로젝트·주 단위**라 회차별이 없다 — 회차 시각 범위로 세션 기록을 묶는 집계를 새로 만든다. 제외 프로젝트 항목을 뺀 뒤 비중을 다시 계산한다.

- [x] **7. 앱 안의 에이전트 루프** [어려움] (2026-09-10, `notes/slice7-에이전트루프.md`, Claude Agent SDK · 검사 `npm run agent:check` 전부 통과 · 화면에서 승인 뒤 착륙까지 확인) — 계획 → 도구 호출 → 결과 관찰 → 다음 행동을 앱 안에서 반복한다(Claude Agent SDK). 완료 기준: "이 플릿의 다음 할 일을 정해줘" 한 번에 읽기 도구를 2종 이상 스스로 부르고, 쓰기 도구 앞에서 승인 대기로 멈추며, 승인 뒤 이어서 끝낸다. 도구 호출과 **그 이유**가 trace 화면에 남는다. 반복 횟수·시간 상한에 닿으면 사유와 함께 멈춘다. 서버를 껐다 켠 뒤에도 그 실행을 이어서 끝낼 수 있다.
  - 함정: `PROBLEM.md` "열려 있는 것" 의 현재 결정(고정 규칙)을 뒤집는 자리다. **판정 규칙은 새로 만들지 않는다** — 규칙이 낸 자격 판정 위에서 순서만 고른다.

