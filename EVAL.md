# 평가 — 무엇을 얼마나 쟀나

2026-09-11 로컬 측정, **2026-09-14 배포본에서 다시 잼**. 기준은 [`PRD.md §8`](PRD.md), 시나리오 세트는 `PRD.md §9`.
화면으로는 [`/eval`](web/app/eval/page.js) 에서 같은 표 넷을 본다.

재현:

```bash
npm run eval             # 표 1 → data/eval/scenarios.json
npm run eval:settings    # 표 2 → data/eval/settings.json
npm run eval:coverage    # 표 3 → data/eval/coverage.json
npm run import:incidents # 표 4 → data/eval/incidents.json   (익명화 설정이 있는 기계에서만)
```

## 판정 일치율을 왜 두 축으로 나눴나

`PRD.md §8` 은 "반입한 회차 기록을 재생해 판정이 당시 결과와 90% 이상 일치" 를 요구한다.
**그 문장을 문자 그대로 할 수 없다** — 회차 기록에는 결과만 있고 **그 시점의 저장소 상태가 없다.**
`src/fleet/runs.mjs` 가 기록에서 뽑는 것은 표 넷과 요약 줄뿐이고, 판정을 다시 돌리려면 그때의
워크스페이스·브랜치·커밋·카드가 있어야 하는데 그것은 어디에도 남아 있지 않다.

그래서 둘로 나눠 잰다.

| 축 | 무엇을 잰다 | 재료 | 한계 |
|---|---|---|---|
| **가. 시나리오 재생 일치율** | 판정이 기대한 결과를 내는가 | 샌드박스 픽스처 (`PRD.md §9` 시나리오 10개) | 표본이 10개다. 기대는 사람이 적었다 |
| **나. 사유 분류 커버리지** | 기록에 남은 사유 문장을 코드가 알아보는가 | 반입한 185회차의 표 넷 | 일치가 아니라 **분류**다. "판정이 옳았나" 는 못 답한다 |

**축 둘을 다시 둘로 나눈 이유** — 파견 보류 사유는 `holdOf`(`src/fleet/source.mjs:61`)가 코드로
바꾸지만, **착륙 막힘 사유는 그 표를 못 쓴다.** `workspaceState` 가 받는 것은 `{ready, blocked,
waiting}` 깃발뿐이고(`src/fleet/source.mjs:60~65`) 문장은 그 옆을 따로 흐른다. 실제로 `holdOf` 를
그대로 먹이면 `막힘` 표의 20건이 전부 `other` 다. 그래서 표마다 분류표를 따로 뒀다
(`src/fleet/reasons.mjs`). **판정 규칙은 건드리지 않았다** — 문장에 라벨을 붙일 뿐이다
(`PRD.md §7`).

## 표 1 — 시나리오 재생 일치

`npm run eval` · 근거 `data/eval/scenarios.json` · 자세한 함정은 `notes/slice8-평가재생.md`.

| | |
|---|---|
| 시나리오 | 10 |
| 재생한 것 | **9** |
| 일치 | **9** |
| 불일치 | 0 |
| 재생 불가 | 1 (9번 — 충돌 해소 2회 실패) |
| **일치율** | **100.0%** (재생 9 중 9) |
| 판정 시간 | 시나리오당 573~650ms · 합계 5.48초 |

- **재생 불가 1건을 분모에서 뺐다.** 9번은 픽스처로 지어내지 않는다 — 샌드박스에는 워커 세션이
  없어 충돌 해소를 시도할 수 없다(`src/fleet/execute.mjs:321~328`). 반입한 185회차의 표에서
  "충돌" 이 든 행도 0개다. 지어내면 100% 가 아니라 거짓말이 된다.
- 8번(카드 질문 대기)의 판정 재료는 워크스페이스 상태가 아니라 재개 갈래(`resume[].action === 'wait'`)와
  카드다. 상태로 대조하면 헛되이 불일치가 난다.
- **기준 세팅(재개 상한 2)에서만 판정한다.** 재개 상한 1에서는 8번의 근거가 사라진다(표 2).

## 표 2 — 세팅 비교

`npm run eval:settings` · 근거 `data/eval/settings.json` · `notes/slice9-세팅비교.md`.
같은 픽스처를 세팅만 갈아 끼워 다시 관찰한 것이다. 판정 규칙이 아니라 **판정에 넘기는 입력값**을 바꿨다.

| 동시 / 재개 상한 | 파견 자격 | 보류 | `cap-project` | 재개 소진 | 사람 호출 |
|---|---|---|---|---|---|
| 2 / 1 | 2 | 9 | 1 | 2 | 8 |
| 2 / 2 | 2 | 9 | 1 | 1 | 7 |
| 2 / 3 | 2 | 9 | 1 | 0 | 6 |
| 3 / 1 | 3 | 8 | 0 | 2 | 7 |
| 3 / 2 | 3 | 8 | 0 | 1 | 6 |
| 3 / 3 | 3 | 8 | 0 | 0 | 5 |
| 5 / 1 | 3 | 8 | 0 | 2 | 7 |
| 5 / 2 | 3 | 8 | 0 | 1 | 6 |
| 5 / 3 | 3 | 8 | 0 | 0 | 5 |

- **표본이 작다.** 기준 세팅에서 `cap-project` 로 떨어지는 슬라이스는 픽스처 전체에서 **1개**다.
  그래서 동시 상한 2→3 에서 이미 0이 되고 3→5 는 아무것도 움직이지 않는다. 수가 안 는다고
  픽스처를 늘리지 않았다 — 늘리면 표 1의 시나리오 표본이 같이 바뀐다.
- 재개 상한 1→3 에서 갈래가 바뀌는 워크스페이스는 둘이다:
  `beacon/slice3` `exhausted → wait → wait` · `cobalt/slice5` `exhausted → exhausted → resume`.
- 에이전트 조합(단일 / 2종)은 픽스처 정의를 바꿔야 해서 **안 했다.** 지금 픽스처가 이미 섞어 쓰는
  두 종(codex 헤드리스 · claude TUI)의 갈래 차이까지가 이번 범위다.

## 표 3 — 사유 분류 커버리지

`npm run eval:coverage` · 근거 `data/eval/coverage.json` · 분류표 `src/fleet/reasons.mjs`.
반입한 **185회차**(파일 10개)의 표 넷에서 뽑았다. (2026-09-14 에 `project-a`·`project-b` 를 통째로 제외해 241 → 185 로 줄었다 — `notes/slice13-공개검수.md`.)

| 표 / 열 | 무엇인가 | 표본 | 고유 문장 | `holdOf` | 전용 분류표 | 못 알아본 것 |
|---|---|---|---|---|---|---|
| 착륙 / `결과` | 결과 갈래 | 48 | 37 | 0.0% | **100.0%** | 0 |
| 파견 / `결과` | 결과 갈래 | 82 | 8 | 0.0% | **100.0%** | 0 |
| 막힘 / `이유` | 착륙 막힘 사유 | 2 | 2 | 0.0% | **100.0%** | 0 |
| 결정 필요 / `내용` | 사람 호출 사유 | 97 | 24 | 23.7% | **100.0%** | 0 |

**착륙·파견 표의 마지막 칸은 사유가 아니다.** `결과`(`머지됨 #22` · `name 실패`)와
`지시`(보낸 프롬프트)뿐이라, 그 둘은 "왜" 가 아니라 "무엇이 됐나" 로 센다. 사유 문장이 있는 칸은
`막힘 / 이유` 와 `결정 필요 / 내용` 둘이다.

갈래별 수:

| 표 | 갈래 |
|---|---|
| 착륙 결과 | `merged` 63 · `push-failed` 3 · `check-failed` 2 · `conflict-failed` 1 · `pr-failed` 1 |
| 파견 결과 | `submitted` 56 · `submitted-hook` 35 · `name-failed` 16 · `wrapper` 9 · `sent` 8 · `create-failed` 2 · `redispatch` 1 |
| 막힘 이유 | `unchecked` 17 · `limit` 1 · `asleep` 1 · `dispatch-unverified` 1 |
| 결정 필요 (`holdOf` 로) | `other` 202 (61.6%) · `limit-hold` 83 · `decision` 43 |

**같은 분류표를 지금 관찰에도 먹였다** — 픽스처의 막힘 3건이 `unchecked` · `asleep` · `limit` 로
갈리고 `other` 는 0이다. 기록(과거 문장)과 판정(지금 문장)이 같은 라벨을 받는다는 뜻이라,
화면의 "막힘 N건" 과 이 표의 수가 같은 것을 센다.

### 헛호출 — 사람을 부른 항목을 사후 분류

`PRD.md §8` 의 헛호출률("사람을 부른 항목 중 실제로 사람 **판단**이 필요했던 비율")이다.
승인 큐는 샌드박스라 표본이 없어, `PLAN.md` 가 정한 대로 **회차 기록의 `결정 필요` 항목**을 쓴다.

같은 항목은 답을 받을 때까지 회차마다 다시 올라온다 — **행 97개는 고유 항목 32개**다. 둘 다 적는다.

| 사람이 할 일 | 고유 항목 | 행 |
|---|---|---|
| **판단** — 답을 골라야 한다 | 13 (40.6%) | 58 (59.8%) |
| **조작** — 답은 정해졌고 손이 필요하다 | 10 (31.3%) | 18 (18.6%) |
| **불필요** — 사람이 할 것이 없다 | 9 (28.1%) | 21 (21.6%) |
| 미분류 | 0 | 0 |

| 코드 | 할 일 | 고유 | 행 | 무엇인가 |
|---|---|---|---|---|
| `limit-hold` | 불필요 | 29 | 83 | 한도가 차 파견을 멈췄다 — 초기화되면 저절로 풀린다 |
| `replan` | 판단 | 13 | 80 | 단계가 끝났다 — 다음 계획은 사람이 짠다 |
| `worker-hands` | 조작 | 12 | 45 | 워커가 카드 `wait` 로 올린 요청 — 사람 손이 필요하다 |
| `decision-tag` | 판단 | 4 | 43 | `[결정 필요]` 태그가 붙은 슬라이스 |
| `worker-decide` | 판단 | 7 | 35 | 워커가 카드 `wait` 로 올린 질문 — 답을 골라야 한다 |
| `stray-branch` | 조작 | 9 | 20 | 브랜치 이름이 `sliceN` 이 아니다 |
| `stray-window` | 조작 | 5 | 12 | 슬라이스를 알 수 없는 창 |
| `land-hold` | 판단 | 1 | 3 | 워커가 착륙을 멈춰 달라고 했다 |
| `plan-dirty` | 판단 | 1 | 3 | 계획이 미커밋 — 검토하고 커밋해야 파견된다 |
| `main-unpushed` | 조작 | 1 | 1 | 본체가 앞서 있다 |
| `limit-wait` | 불필요 | 1 | 1 | 상대 에이전트도 한도라 기다린다 |
| `handoff` | 불필요 | 1 | 1 | 인계했다는 보고 — 실행은 이미 됐다 |
| `resume-exhausted` | 판단 | 1 | 1 | 재개 2회 실패 |

**헛호출률 = 판단 40.6%(고유) · 59.8%(행). 목표 80% 에 못 미친다.**
손이라도 필요했던 것(판단+조작)까지 세도 71.9% · 78.4% 로 여전히 못 넘는다 — 행 기준으로는 1.6%p 차다.

- 가장 큰 몫은 **`limit-hold` 21행(고유 9)** — 한도가 차 파견을 멈췄다는 보고다. 초기화되면
  저절로 풀리고 그 사이 사람이 할 것이 없는데도 `결정 필요` 로 올라온다. 이것 하나를 사람 호출에서
  빼면 판단 비율은 행 기준 59.8% → 76.3% 가 된다(58/76).
- **판정 갈림 하나를 적어 둔다**: 한도 보고를 받은 사람이 "다른 에이전트로 인계할까" 를 정할 수는
  있다. 그 여지를 판단으로 세면 수가 달라진다. 여기서는 **인계를 플릿이 스스로 하므로**
  (`handoff` 항목이 그 증거다) 불필요로 셌다.
- `worker-hands` / `worker-decide` 를 가른 것은 **휴리스틱**이다 — 문장에 사람이 할 동작이
  적혀 있으면 조작으로 본다(`src/fleet/reasons.mjs` `WORKER_HANDS`). 항목별 라벨은
  `data/eval/coverage.json` 의 `calls.items` 에 그대로 있어 사람이 되짚을 수 있다.

## 표 4 — 사고 분류

`npm run import:incidents` · 근거 `data/eval/incidents.json` · 원본은 운영 저장소의 규칙 근거 표.
익명화 규칙은 `ANONYMIZATION.md` — **익명화는 이번이 세 번째다**(코드 · 회차 기록 · 사고 사례).

원본 표 **30행**(최대 번호 30) 중 제외 목록 프로젝트의 이름이 든 행 **1개**를 통째로 뺐다(§1 —
이름만 지우는 것이 아니다). 반입 **29행**. 금칙어 스캔 0건.

**2026-09-14 추가 제외 — 23행이 남았다.** 소유자가 `project-a`·`project-b` 를 통째로 제외하기로 해
그 프로젝트가 사건의 무대인 **6건(n=8·15·22·24·26·30)** 을 뺐다. 본문이 그 프로젝트의 내용을
다시 들여오기 때문이다(§1). n=22 만 주제가 `SP-sync` 이고 그 프로젝트는 부차 예시였다 — 되살리려면
그 절만 지우면 된다. 뺀 번호는 `data/eval/incidents.json` 의 `excludedLater` 에 남겼다.
아래 수는 **모두 제외 뒤의 값**이다.

**표 행 수는 사고 수가 아니다.** 같은 표에 사용자 결정·점검 결과·기능 설명이 섞여 있다.

| 갈래 | 수 |
|---|---|
| 사고 | **13** (제외 전 19) |
| 사고 아님 (사용자 결정·요청 4 · 점검 결과 3 · 기능 2 · 운영 결정 1) | 10 (제외로 줄지 않았다 — 6건 모두 `사고` 였다) |

| 원인 단계 | 건수 | 번호 |
|---|---|---|
| 관찰 | **0** | — (8·15 가 제외됐다) |
| 판정 | 4 | 2, 4, 5, 25 |
| 실행 | **5** | 10, 13, 21, 23, 27 |
| 사람 개입 설계 | 4 | 7, 12, 14, 16 |

- 단계 라벨은 **사람이 읽고 붙였다.** 규칙이 아니라 판단이므로 근거는 각 행의 원문(익명화본)이
  진다 — `data/eval/incidents.json` 의 `cases[].text`. 원본에 행이 늘면 `미분류` 로 세어 보고한다.
- `PRD.md §9` 는 "사고 28건" 이라고 적었다. 그때 세지 않은 것이 섞여 있었다 — 반입 시점의 실제 수는
  **19건**이고, 제외 뒤 이 저장소에 남은 것은 **13건**이다.
- 사람 개입 설계 4건 중 둘(12 · 16)이 **헛호출**이다. 표 3의 `limit-hold` 21행이 같은 종류의
  반복이라, 두 표가 같은 곳을 가리킨다.
- **관찰 단계가 0이 된 것은 그 단계에 사고가 없다는 뜻이 아니다** — 있던 2건이 제외 프로젝트의
  것이었을 뿐이다. 표본이 줄면 이런 빈 칸이 생긴다.

## `PRD.md §8` 다섯 기준 — 로컬 재생 (2026-09-11)

| 기준 | 목표 | 지금 | 됐나 |
|---|---|---|---|
| 판정 일치율 | 90% 이상 | **가. 재생 100.0%** (재생 9 / 일치 9) · **나. 사유 분류 100.0%** (표본 229행, `other` 0) | ✅ 단 **표본이 작고**(시나리오 9) 축 나는 일치가 아니라 분류다 |
| 헛호출률 | 80% 이상 | **40.6%**(고유) · **59.8%**(행). 판단+조작으로 세면 71.9% · 78.4% | ❌ 못 넘는다 — `limit-hold` 21행이 원인 |
| 회차 소요 | 판정 10초 이내 | **0.65초** (가장 오래 걸린 시나리오, 상한의 6.5%) | ✅ |
| 관찰 가능성 | 모든 도구 호출과 사유를 볼 수 있다 | 회차 상세 **241/241** 이 뜨고, 그 회차에 기록된 막힘·결정 필요 사유가 **하나도 빠짐없이** 화면에 있다(2026-09-11 실측 — **제외 전 241개 기준**, 아래) | ✅ 회차 기록 쪽만. 에이전트 루프의 도구 호출·사유는 `/agent/[id]` 가 따로 낸다 |
| 배포 | 링크만으로 끝까지 실행 | 2026-09-14 완료 — 아래 배포본 표 | ✅ |

**관찰 가능성을 잰 법**: 로컬에서 `next start`(127.0.0.1:3118, 이 워크트리의 `web/`)로 띄우고
241개 회차 상세를 전부 받아, 그 회차의 `막힘 / 이유` 와 `결정 필요 / 내용` 문장이 응답 본문에
있는지 대조했다 — 200 응답 241건, 사유 미표시 0건. 스크립트는 임시로 돌렸고 남기지 않았다.
2026-09-14 의 제외로 회차가 185개가 됐다. 전수 대조를 다시 돌리지 않았으므로 **이 값은 241개 시점의 것**이고,
제외는 행을 지운 것이라 남은 회차의 표시 방식은 달라지지 않는다(배포본 표본 11/11 이 그것을 다시 확인했다).

**참고**: `회차 소요` 는 판정 시간이다. 실제 운영 회차의 벽시계 시간(착륙·파견 실행 포함)은
가운뎃값 1분 5초 · 최장 7분 11초로 따로 있다(`/eval` 위쪽, `data/usage/cycles.json`).

## `PRD.md §8` 다섯 기준 — 배포본에서 다시 잰 값 (2026-09-14)

서비스 `https://fleet-console-d7c5.onrender.com` (Render Free · Docker · 0.1 CPU · 512MB).
배포본이 도는 커밋은 `56beb1f` 이고, 그 뒤 로컬 커밋 셋은 문서·노트만이라 **코드는 위 표와 같다**
(`git diff origin/main..HEAD -- src/ web/ scripts/` 가 비어 있다). 그래서 스크립트로만 나오는 두 축
(판정 일치율 · 헛호출률)은 같은 값이고, 하드웨어와 화면에 걸리는 세 축을 배포본에서 새로 쟀다.

| 기준 | 목표 | 배포본 값 | 됐나 |
|---|---|---|---|
| 판정 일치율 | 90% 이상 | **재생 100.0%** (9/9, 최장 683ms) — 같은 커밋에서 `npm run eval` 재실행. 사유 분류 100.0%(229행) | ✅ 표본은 위 표와 같다 |
| 헛호출률 | 80% 이상 | **40.6%**(고유) · **59.8%**(행) — 반입 기록 기반이라 배포로 달라지지 않는다 | ❌ 원인은 `limit-hold` 21행 |
| 회차 소요 | 판정 10초 이내 | **1.0초** — 배포본 에이전트 실행에서 `fleet_status` 1,001ms · `fleet_slices` 894ms(0.1 CPU 실측). 로컬 재생 최장 683ms | ✅ 상한의 10% |
| 관찰 가능성 | 모든 도구 호출과 사유 | `/agent/7b1bbb0c…` 에서 도구 **5회 전부** 이름·인자·**이유**·결과·소요가 보인다(거부·승인 대기 포함). 회차 상세는 배포본에 **185개** 링크가 있고 **표본 11개 전부** 200 + 사유 절 표시 | ✅ 회차 상세는 표본이다. 표본 11개는 제외 전(241) 시점에 쟀고, 제외 반영 재배포 뒤 링크 수가 **185** 로 맞는 것을 다시 확인했다(2026-09-14) |
| 배포 | 링크만으로 끝까지 실행 | **✅ 2026-09-14** — 링크만으로 실행 → 승인 대기 → (15분 절전) → 승인 → 착륙 `머지 완료 (PR #1)` → 이어서 끝내기 `끝남`. 도구 5회 · 턴 6 · $0.0033 | ✅ 다른 기기(휴대폰) 확인은 별도 |

**절전을 건너뛴 것이 이 표의 핵심이다.** 승인 대기 실행을 남긴 채 15분을 방치해 컨테이너가 잠들었고,
다시 열었을 때(첫 접속 52.3초) 실행·승인 큐·오늘 실행 횟수가 그대로였다. 재개 구간에서 모델이 같은
쓰기 도구를 다시 부른 것은 **거부**됐다 — 사람이 큐에서 이미 실행해 워크스페이스가 없었다.
실측 상세는 `notes/slice12-컨테이너배포.md` 4차 절, 응답 시간은 `README.md`.

## 표본 수 한곳에

| 무엇 | 수 |
|---|---|
| 시나리오 | 10 (재생 9) |
| 세팅 조합 | 9 (+ 기준 1) |
| 반입 회차 | **185** (파일 10) — 제외 전 241 |
| 분류한 사유·결과 칸 | **229** (착륙 48 · 파견 82 · 막힘 2 · 결정 필요 97) — 제외 전 545 |
| 사람 호출 고유 항목 | **32** — 제외 전 85 |
| 픽스처 워크스페이스 | 5 · 미완 슬라이스 11 · `cap-project` 표본 1 (픽스처라 제외와 무관) |
| 사고 사례 | 원본 30행 → 반입 29행 → **제외 후 23행 → 사고 13건** |

**2026-09-14 에 표본이 줄었다.** 소유자가 `project-a`·`project-b` 를 통째로 제외해(`ANONYMIZATION.md §3`,
근거 `notes/slice13-공개검수.md`) 회차 56개와 표 행 316개가 빠졌다. `§3` 대로 **줄어든 수를 그대로** 적고
원래 수는 각주가 아니라 같은 칸에 병기했다. 막힘 표본이 20 → 2 로 떨어져 그 축은 이제 거의 아무것도
보여주지 못한다 — 늘리려고 픽스처를 건드리지 않았다.

## 다음에 고칠 것

1. **`limit-hold` 를 사람 호출에서 내린다.** 헛호출률을 끌어내리는 것이 이것 하나다. 초기화 시각이
   지나면 저절로 풀리므로 "볼 것" 이 아니라 "기다리는 것" 으로 나뉘어야 한다.
2. **`cap-project` 표본 1개.** 세팅 비교가 이 축에서는 사실상 아무것도 못 보여준다. 픽스처를
   늘리는 대신 반입한 회차 기록에서 그 사유를 뽑아 세는 쪽이 표본이 크다.
3. **축 나는 분류일 뿐 일치가 아니다.** 판정이 옳았는지 재려면 그 시점의 저장소 상태가 기록에
   남아야 한다 — 지금 회차 기록 형식으로는 불가능하다.
