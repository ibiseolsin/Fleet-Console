# 문제정의 — Fleet Console

작성 2026-09-10. **무엇을 왜 만드는가**만 다룬다. 기능·화면은 `PRD.md`, 구현 순서는 `PLAN.md`.

## 한 문장

**여러 AI 코딩 에이전트를 동시에 굴리는 개발자가, 어느 작업을 누구에게 맡기고 · 끝난 것을 언제 반영하고 ·
막힌 것을 어떻게 넘길지 하루 종일 터미널을 지켜보며 판단하는 일을**, 규칙으로 판정하고 위험한 순간에만
사람을 부르는 시스템에게 넘긴다.

## 도메인

한 사람이 프로젝트 7개를 동시에 진행하고, 각 프로젝트의 작업은 "슬라이스" 단위로 쪼개져 있으며,
슬라이스마다 격리된 작업 공간에서 AI 에이전트(Claude Code / Codex / Antigravity) 세션이 하나씩 돈다.

한 번의 운영 회차에서 판단해야 하는 것:

| 판단 | 근거로 봐야 하는 것 |
|---|---|
| 끝난 작업을 반영할까 (착륙) | 완료 체크 · 커밋 여부 · 작업 트리 청결 · 세션 유휴 · 병합 가능 여부 |
| 막힌 것을 다시 깨울까 (재개) | 사용량 한도 초기화 시각 · 재시도 횟수 |
| 다른 에이전트에게 넘길까 (인계) | 한도가 언제 풀리는지 · 상대 에이전트가 여유 있는지 |
| 새 작업을 맡길까 (파견) | 계획서의 미완 슬라이스 · 선행 의존 · 병렬 가능 여부 · 동시 실행 상한 |
| 사람을 불러야 하나 | 결정 필요 표시 · 카드의 질문 대기 · 충돌 해소 실패 |

프로젝트 7개 × 판단 5종을 매번 손으로 하면 하루가 다 간다. 그래서 이 판단은 이미 자동화돼 있다 —
이 프로젝트는 **그 자동화를 사람과 외부 에이전트가 쓸 수 있게 여는 일**이다.

## 지금은 어떻게 하고 있나

`sp-sync` 라는 CLI 가 회차를 돈다: **본체 동기화 → 착륙 → 재개 → 인계 → 파견 → 보고.**

- 판정은 부수효과 없는 순수 함수가 한다 (`dispatchPlan`, `resumePlan`, `landCheck` 등).
  테스트 455개가 이 규칙 분기를 검증한다.
- 상태는 네 곳에 나뉜다 — 세션 기록, 계획서의 완료 체크, 재개 대기 테이블, 사람이 읽는 진행 카드.
- 회차마다 보고서가 쌓인다 — `착륙 N · 파견 N · 막힘 N · 결정 필요 N` 요약과 표 네 개. 2026-08-28 이후 파일 17개에 회차 258개.
- 실패는 규칙이 된다. 잘못 돌아간 사례 28건이 "무엇이 잘못됐고 그래서 어떤 규칙이 생겼나" 로 남아 있다.

즉 **루프도, 상태 관리도, 실패 처리도, 사람 개입 지점도 이미 돌고 있다.**

## 무엇이 불편한가

관찰된 사실만 적는다.

1. **관찰이 전부 로컬 마크다운이다.** 회차 보고도 진행 카드도 로그도 파일이다. 무슨 일이 벌어지는지
   보려면 그 기계 앞에 앉아 파일을 열어야 한다.
2. **승인이 알림이 아니라 침묵이다.** "결정 필요" 항목은 보고서 표에 쌓일 뿐 사람을 부르지 않는다.
   사람이 열어보기 전까지 그 프로젝트는 조용히 멈춰 있다.
3. **외부 에이전트가 이 시스템을 쓸 수 없다.** 판정 함수가 잘 분리돼 있는데도 CLI 로만 열려 있어,
   다른 에이전트가 "지금 플릿 상태를 보고 다음 할 일을 정해줘" 를 할 방법이 없다.
4. **비용이 코드 밖에 있다.** 토큰 사용량은 사람이 따로 측정해 노트에 적었다. 회차가 스스로 기록하지
   않으므로 "이 파견이 얼마짜리였나" 를 되짚을 수 없다.
5. **기계를 벗어나지 못한다.** 같은 방식으로 일하는 다른 사람에게 보여줄 수도 넘겨줄 수도 없다.

## 왜 에이전트인가

플릿 운영은 답을 받아서 끝나지 않는다. **작업 공간이 실제로 만들어지고, 브랜치가 병합되고, 멈춘 세션이
다시 깨어나야** 끝난다. 최종 산출물은 대화가 아니라 병합된 커밋과 갱신된 계획서다.

회차마다 봐야 할 것도 다르다. 착륙 하나로 끝나는 회차가 있고, 인계 두 건에 충돌 해소까지 가는 회차가
있다. **몇 번 무엇을 할지 미리 정할 수 없으므로** 관찰 → 판정 → 실행 → 재관찰을 반복하는 루프가 필요하다.

## 성공했다는 것은

`PRD.md` 에서 측정 가능한 문장으로 다듬는다.

- 실제 회차 기록으로 재생했을 때 **판정이 사람의 판단과 일치한 비율**
- 한 회차를 끝내는 데 든 **시간 · 토큰 · 비용**
- 사람이 불려 나온 횟수 중 **실제로 사람 판단이 필요했던 비율**(헛호출률)
- 중단된 작업이 **이어서 완료된 비율**

## 하지 않을 것

- **`sp-sync` 코드를 고치지 않는다.** 455개 테스트로 지켜지는 코드다. 읽고 감싸기만 한다.
  유일한 예외는 공개를 위한 익명화 치환이다 (`ANONYMIZATION.md`) — 동작은 바꾸지 않는다.
- **판정 규칙을 새로 만들지 않는다.** 28건의 사고에서 나온 규칙이라 지금 형태에 이유가 있다.
- **배포본이 실제 운영 중인 플릿을 조작하지 않는다.** 배포된 화면은 기록을 보여주고 승인을 받을 뿐,
  남의 기계에서 남의 저장소를 건드리지 않는다.
- 작업 공간 삭제 · 강제 푸시처럼 되돌릴 수 없는 조작은 도구로 노출하지 않는다.

## 열려 있는 것

- **판정의 주체** — 지금은 LLM 이 아니라 고정 규칙이 판정한다. 이를 "규칙 엔진 + 그 위의 오케스트레이터"
  로 설명할지, 도구를 골라 회차를 진행하는 얇은 LLM 루프를 얹을지. **결정(2026-09-10): 둘 다 —
  판정은 고정 규칙이 그대로 하고, 그 위에서 무엇을 먼저 할지만 얇은 LLM 루프가 고른다.**
  플릿의 워커들이 진짜 LLM 에이전트이고, 이 시스템은 그들을 조율하는 멀티 에이전트 레이어다.
- **마감일 미정** — 정해지면 슬라이스 6개를 역산한다.

## 다음 단계

`PRD.md` — 타겟 유저 / 워크플로 설계 / 도구 계획 / 사람 개입 지점 / MVP·화면 + 평가 시나리오.
